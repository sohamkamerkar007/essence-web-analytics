const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)],money=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
const store=(k,d)=>{try{return JSON.parse(localStorage.getItem(k))||d}catch{return d}},save=(k,v)=>localStorage.setItem(k,JSON.stringify(v)),cart=()=>store('essenceCart',[]);
function trackEvent(eventName,eventData={}){console.log('[Essence analytics]',eventName,eventData);(window.dataLayer=window.dataLayer||[]).push({event:eventName,...eventData})}function item(p,quantity=1){return {item_id:p.id,item_name:p.name,item_brand:p.brand,item_category:'Perfume',price:p.price,quantity}}
function art(p,big=''){return `<div class="product-image ${big}"><img src="${p.image}" alt="${p.brand} ${p.name} bottle" loading="lazy"></div>`}function header(){return `<header><a class="logo" href="index.html">ESSENCE</a><button class="menu" aria-label="Open menu">☰</button><nav><a href="index.html">Home</a><a href="shop.html">Shop</a><a href="about.html">About</a></nav><div class="head-actions"><form class="mini-search" action="shop.html"><input name="q" aria-label="Search fragrances" placeholder="Search"><button>⌕</button></form><a href="cart.html" class="count">Cart <sup>${cart().reduce((n,x)=>n+x.qty,0)}</sup></a></div></header>`}function footer(){return `<footer><div><a class="logo" href="index.html">ESSENCE</a><p>Find the scent that defines you.</p></div><div><h4>Contact</h4><p>hello@essence.demo</p><p>Mon–Fri, 10:00–18:00</p></div><div class="foot-bottom">© 2026 Essence. Academic demo project. Not affiliated with the brands shown.</div></footer>`}
function shell(content,crumb=''){document.body.innerHTML=header()+`<main>${crumb?`<div class="crumb wrap">${crumb}</div>`:''}${content}</main>`+footer();$('.menu')?.addEventListener('click',()=>$('nav').classList.toggle('open'));bindButtons()}function toast(t){const x=document.createElement('div');x.className='toast';x.textContent=t;document.body.append(x);setTimeout(()=>x.remove(),2200)}function addCart(id,qty=1){const p=products.find(x=>x.id===id),c=cart(),x=c.find(x=>x.id===id);if(!p)return;x?x.qty+=qty:c.push({id,qty});save('essenceCart',c);$('.count sup').textContent=c.reduce((n,a)=>n+a.qty,0);trackEvent('add_to_cart',{currency:'INR',value:p.price*qty,items:[item(p,qty)]});toast(`${p.name} added to cart`)}function bindButtons(){$$('[data-add]').forEach(b=>b.onclick=()=>addCart(b.dataset.add));$$('[data-select]').forEach(a=>a.onclick=()=>{const p=products.find(x=>x.id===a.dataset.select);trackEvent('select_item',{items:[item(p)]})})}
function card(p){return `<article class="card"><a class="card-art" href="product.html?id=${p.id}" data-select="${p.id}">${art(p)}</a><div class="card-body"><span class="eyebrow">${p.brand}</span><a class="pname" href="product.html?id=${p.id}" data-select="${p.id}">${p.name}</a><div class="rating">★ ${p.rating} · ${p.fragranceFamily}</div><div class="card-bottom"><strong>${money(p.price)}</strong><button data-add="${p.id}">Add to cart</button></div><a class="view-details" href="product.html?id=${p.id}" data-select="${p.id}">View details →</a></div></article>`}function grid(a,msg='No fragrances found'){return a.length?`<div class="grid">${a.map(card).join('')}</div>`:`<div class="empty"><h3>${msg}</h3><a class="button dark" href="shop.html">Browse all</a></div>`}
function renderHome(){shell(`<section class="hero"><div><span class="eyebrow">ESSENCE</span><h1>Find the scent<br><i>that defines you.</i></h1><p>Discover a considered collection of iconic fragrances, ready for your next signature moment.</p><a class="button light" href="shop.html">Shop Fragrances</a></div><div class="hero-art">${art(products.find(p=>p.id==='tf-oud-wood'),'big')}</div></section><section class="section wrap"><div class="section-head"><div><span class="eyebrow">Popular now</span><h2>Featured fragrances</h2></div><a href="shop.html">View all →</a></div>${grid(products.slice(0,6))}</section><section class="brand-band"><div class="wrap"><span class="eyebrow">Explore the collection</span><h2>Shop by brand</h2><div class="brand-row">${brands.map(b=>`<a href="shop.html?brand=${encodeURIComponent(b)}"><strong>${b}</strong><small>${products.filter(p=>p.brand===b).length} fragrances →</small></a>`).join('')}</div></div></section><section class="section wrap about-short"><span class="eyebrow">About Essence</span><h2>Less noise. More feeling.</h2><p>Essence is a simple fragrance discovery store, created as an academic web analytics project.</p><a href="about.html">About Essence →</a></section>`)}function renderAbout(){shell(`<section class="intro wrap"><span class="eyebrow">About Essence</span><h1>Fragrance is<br><i>personal.</i></h1><p>Essence makes fragrance discovery simple: browse, choose and place a demo order in just a few steps.</p><h2>Contact</h2><p>hello@essence.demo<br>Mon–Fri, 10:00–18:00</p></section>`,'About')}
function renderCart(){const rows=cart().map(x=>({...x,p:products.find(p=>p.id===x.id)})).filter(x=>x.p),total=rows.reduce((n,x)=>n+x.p.price*x.qty,0);trackEvent('view_cart',{currency:'INR',value:total,items:rows.map(x=>item(x.p,x.qty))});shell(`<section class="intro compact wrap"><span class="eyebrow">Your cart</span><h1>Shopping bag</h1></section><section class="cart-layout wrap"><div>${rows.length?rows.map(x=>`<div class="cart-row"><a class="cart-art" href="product.html?id=${x.p.id}">${art(x.p)}</a><div><span class="eyebrow">${x.p.brand}</span><h3>${x.p.name}</h3><div class="qty"><button data-cart="dec" data-id="${x.id}">−</button><span>${x.qty}</span><button data-cart="inc" data-id="${x.id}">+</button></div></div><strong>${money(x.p.price*x.qty)}</strong><button class="remove" data-cart="remove" data-id="${x.id}">Remove</button></div>`).join(''):`<div class="empty"><h3>Your cart is empty.</h3><a class="button dark" href="shop.html">Continue shopping</a></div>`}</div>${rows.length?`<aside class="summary"><h2>Summary</h2><div><span>Subtotal</span><strong>${money(total)}</strong></div><hr><div class="total"><span>Total</span><strong>${money(total)}</strong></div><a class="button dark full" href="checkout.html">Checkout</a></aside>`:''}</section>`,'Cart');$$('[data-cart]').forEach(b=>b.onclick=()=>cartAction(b.dataset.cart,b.dataset.id))}function cartAction(a,id){let c=cart(),i=c.findIndex(x=>x.id===id),p=products.find(x=>x.id===id);if(a==='remove'){c.splice(i,1);trackEvent('remove_from_cart',{items:[item(p,1)]})}else if(a==='inc')c[i].qty++;else if(a==='dec'){c[i].qty--;if(c[i].qty<1)c.splice(i,1)}save('essenceCart',c);renderCart()}
function renderCheckout(){const rows=cart().map(x=>({...x,p:products.find(p=>p.id===x.id)})).filter(x=>x.p),total=rows.reduce((n,x)=>n+x.p.price*x.qty,0);if(!rows.length){location.href='cart.html';return}trackEvent('begin_checkout',{currency:'INR',value:total,items:rows.map(x=>item(x.p,x.qty))});shell(`<section class="intro compact wrap"><span class="eyebrow">Checkout</span><h1>Almost yours</h1></section><section class="checkout wrap"><form id="checkout"><fieldset><legend>Customer information</legend><div class="fields">${[['Full name','name','text'],['Email','email','email'],['Phone','phone','tel'],['City','city','text']].map(x=>`<label>${x[0]}<input required name="${x[1]}" type="${x[2]}"></label>`).join('')}</div></fieldset><fieldset><legend>Payment method</legend><label class="choice"><input required type="radio" name="payment" value="UPI"> UPI</label><label class="choice"><input required type="radio" name="payment" value="Cash on Delivery"> Cash on Delivery</label></fieldset><button class="button dark full">Place Order</button></form><aside class="summary"><h2>Your order</h2>${rows.map(x=>`<div><span>${x.p.name} × ${x.qty}</span><strong>${money(x.p.price*x.qty)}</strong></div>`).join('')}<hr><div class="total"><span>Total</span><strong>${money(total)}</strong></div></aside></section>`,'Checkout');$('#checkout').onsubmit=e=>{e.preventDefault();const payment=new FormData(e.target).get('payment'),id=`ESS-${Math.floor(10000+Math.random()*90000)}`;trackEvent('add_payment_info',{payment_method:payment,currency:'INR',value:total,items:rows.map(x=>item(x.p,x.qty))});trackEvent('purchase',{transaction_id:id,currency:'INR',value:total,payment_method:payment,items:rows.map(x=>item(x.p,x.qty))});save('essenceLastOrder',{id,total,payment});save('essenceCart',[]);location.href=`order-success.html?id=${id}`}}
function renderSuccess(){const o=store('essenceLastOrder',{});shell(`<section class="success-page wrap"><div>✓</div><span class="eyebrow">Order placed successfully</span><h1>Thank you for shopping<br><i>with Essence.</i></h1><p>Order <strong>#${o.id||new URLSearchParams(location.search).get('id')}</strong><br>Payment method: ${o.payment||'—'}<br>Order total: ${money(o.total||0)}</p><a class="button dark" href="shop.html">Continue shopping</a></section>`,'Order confirmation')}
document.addEventListener('DOMContentLoaded',()=>{const page=document.body.dataset.page;trackEvent('page_view',{page});({home:renderHome,about:renderAbout,cart:renderCart,checkout:renderCheckout,success:renderSuccess})[page]?.()});
