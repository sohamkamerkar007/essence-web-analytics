function renderProduct(){const p=products.find(x=>x.id===new URLSearchParams(location.search).get('id'))||products[0];trackEvent('view_item',{currency:'INR',value:p.price,items:[item(p)]});shell(`<section class="product-detail wrap"><div class="detail-art">${art(p,'big')}</div><div class="detail-copy"><span class="eyebrow">${p.brand}</span><h1>${p.name}</h1><div>★ ${p.rating} · ${p.fragranceFamily}</div><strong class="price">${money(p.price)}</strong><p class="lead">${p.description}</p><div class="detail-actions"><button class="button dark" id="add">Add to cart</button><button class="button outline" id="buy">Buy now</button></div></div></section>`,'Shop / '+p.name);$('#add').onclick=()=>addCart(p.id);$('#buy').onclick=()=>{trackEvent('buy_now',{items:[item(p)]});addCart(p.id);location.href='checkout.html'}}document.addEventListener('DOMContentLoaded',renderProduct);
